# 哲学者AI相談室 プロトタイプ

ブラウザだけで動く静的なデモです。哲学者を選び、悩みを入力すると口調を模した回答が表示されます。

## 使い方

```bash
python -m http.server 8000
```

ブラウザで `http://localhost:8000/philosopher_ai_app/` にアクセスしてください。

## メモ

- 現状は固定のテンプレート回答を返すだけのモックです。
- 実際のAI連携時は、APIからの応答を `responseTemplate` の代わりに差し込む構成です。
